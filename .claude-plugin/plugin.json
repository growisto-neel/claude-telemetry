{
  "name": "growisto-claude-telemetry",
  "version": "0.2.0",
  "description": "Records Claude Code usage (prompt preview, skill, folder, session) and reports it to Growisto analytics. Never records full prompts, responses, or file contents.",
  "author": {
    "name": "Growisto",
    "email": "neel.thakkar@growisto.com"
  },
  "homepage": "https://github.com/growisto-neel/claude-telemetry",
  "license": "UNLICENSED",
  "keywords": ["telemetry", "analytics", "internal"],

  "userConfig": {
    "ga4MeasurementId": {
      "type": "string",
      "title": "GA4 measurement ID",
      "description": "The G- ID of the Growisto analytics property. Ask Neel if you do not have it.",
      "default": ""
    },
    "ga4ApiSecret": {
      "type": "string",
      "title": "GA4 API secret",
      "description": "Write credential for the analytics property. Stored by Claude Code, not written to disk in plain text.",
      "sensitive": true,
      "default": ""
    },
    "userEmail": {
      "type": "string",
      "title": "Work email",
      "description": "Leave blank to use your git config user.email. Set it if your git identity is a personal address.",
      "default": ""
    },
    "team": {
      "type": "string",
      "title": "Team",
      "description": "Optional. Used to group usage by team in dashboards.",
      "default": ""
    },
    "promptCapture": {
      "type": "string",
      "title": "Prompt capture mode: preview or hash",
      "description": "Type 'preview' to keep the first 100 characters of each prompt, or 'hash' to keep only a one-way hash and no readable text. Leave blank for 'preview'. Anything unrecognised is treated as 'hash', so a typo can only narrow what is collected.",
      "default": "preview"
    },
    "pathCapture": {
      "type": "string",
      "title": "Folder capture mode: full, basename, or none",
      "description": "Type 'full' to record the working directory path, 'basename' for only the folder name, or 'none' for no location at all. Leave blank for 'full'. Anything unrecognised is treated as 'none', so a typo can only narrow what is collected.",
      "default": "full"
    }
  }
}
